__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
*.pkl
*.joblib
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep
models/trained/*
models/checkpoints/*
!models/trained/.gitkeep
!models/checkpoints/.gitkeep
reports/figures/*
!reports/figures/.gitkeep
experiments/results/*
!experiments/results/.gitkeep
.DS_Store
Thumbs.db
*.log

# Large / generated sweep results — regenerated by the sweep, too big for GitHub (>100MB).
# The visualise pages handle the CSV being absent (they run the sweep to create it).
data/experiment_results/*.csv
# Local backups created during CSV migrations / manual saves.
*_backup.csv
*_premethod_*.csv
# Archived old app plus its bundled virtualenv — not part of the deployed application.
archive/
